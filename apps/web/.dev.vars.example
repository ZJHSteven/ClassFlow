# Cloudflare Worker 本地开发 / 生产部署都需要的环境变量
# 本地可复制为 `.dev.vars`
# 注意：
# 1. `BACKEND_BASE_URL` 指向后端 Tunnel 域名
# 2. `BACKEND_TOKEN` 必须与后端 `CLASSFLOW_BEARER_TOKEN` 完全一致
# 3. 如果后端 Tunnel 域名已经接入 Cloudflare Access，则还需要同时填写
#    `CF_ACCESS_CLIENT_ID` / `CF_ACCESS_CLIENT_SECRET`
# 4. `ARTIFACT_PROXY_TOKEN` 是“后端 -> Worker 私有产物接口”使用的单独密钥
# 5. 生产环境还需要在 `wrangler.toml` 中把 `ARTIFACTS` 绑定到真实 R2 bucket

BACKEND_BASE_URL=https://classflow-backend.example.com
BACKEND_TOKEN=replace-with-backend-bearer-token
CF_ACCESS_CLIENT_ID=replace-with-cloudflare-access-client-id
CF_ACCESS_CLIENT_SECRET=replace-with-cloudflare-access-client-secret
ARTIFACT_PROXY_TOKEN=replace-with-worker-artifact-token
